# Baseline needs exactly one dependency.
requests>=2.28.0

# Optional — free Google Translate wrapper, no key/account/cost. Powers the
# Korean translation shown under sample quotes. Skipped gracefully if absent.
deep-translator>=1.11.4

# Optional — only for `--analyzer llm` (Claude-assisted analysis).
# Needs ANTHROPIC_API_KEY and is a paid API; unrelated to translation now.
# anthropic>=0.40.0
